apiVersion: v1
kind: Service
metadata:
  name: thai-menu-api-service
spec:
  selector:
    app: thai-menu-api
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30007
  type: NodePort
